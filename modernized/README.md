# index

This repo contains a legacy HTML export.

To generate a modernized version of the site into `modernized/`, run:

```bash
npm run modernize
```

You can also choose a different output folder:

```bash
node tools/modernize-html.mjs preview-site
```
